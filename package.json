{
  "name": "salesaiguide",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "python3 scripts/indexation_gate.py --site-dir . --out-dir . --base-url https://salesaiguide.com; test -f sitemap.xml && test -f _headers",
    "gate": "python3 scripts/indexation_gate.py --site-dir . --out-dir . --base-url https://salesaiguide.com || true",
    "test": "python3 scripts/test_slop_signals.py",
    "aros": "node scripts/aros.js",
    "funnel": "python3 scripts/build_revenue_funnel.py",
    "probe": "python3 scripts/distribution_probe.py",
    "affiliates": "python3 scripts/affiliate_manager.py"
  },
  "dependencies": {
    "canvas": "^3.2.1"
  }
}
